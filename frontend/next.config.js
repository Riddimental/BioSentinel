/** @type {import('next').NextConfig} */
const nextConfig = {
  // eslint: {
  //   // Ignorar ESLint en el build de producción
  //   ignoreDuringBuilds: true,
  // },
  // // silencia TS en build
  // typescript: { ignoreBuildErrors: true },
};

module.exports = nextConfig;
